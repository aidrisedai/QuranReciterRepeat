package com.repeatquran.playback;

import org.junit.Test;
import org.junit.runner.RunWith;
import org.robolectric.RobolectricTestRunner;

import static org.junit.Assert.assertTrue;

/**
 * Placeholder test: Range backend has been re-architected and legacy planner is removed.
 * This ensures the test suite remains green while new tests are introduced.
 */
@RunWith(RobolectricTestRunner.class)
public class RangePlannerTest {
    @Test
    public void rangeBackendRemoved_placeholder() {
        assertTrue(true);
    }
}

